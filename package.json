{
  "name": "vscode-spring-chat",
  "displayName": "Spring and Springboot Chat Participant",
  "description": "Spring and Springboot Chat Participant that will answer your Spring and Springboot questions.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.94.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      
    ],
    "chatParticipants": [
      {
        "id": "vscode-spring-chat",
        "name": "spring",
        "description": "Spring and Springboot Chat Participant"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "extensionDependencies": [
    "github.copilot-chat"
  ],
  "devDependencies": {
    "@types/vscode": "^1.94.0",
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^7.14.1",
    "@typescript-eslint/parser": "^7.11.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.4.0"
  }
}
